"""
__init__.py - the graph schema package.

`graph_model.py` is the model as data (`NODE_KEYS`, `RELATIONSHIPS`),
`schema.cypher` is the constraints and indexes, `graph_model.md` is the human
deliverable. The three do not drift - `kg/loader/tests` asserts it.

Author: Colile
"""
