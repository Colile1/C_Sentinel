"""
__init__.py - the demonstrated queries and the graph service API.

The `.cypher` files each open with the plain-English question they answer;
`queries.py` loads them by name; `graph_service.py` exposes each as an HTTP
endpoint so they are reachable from the RAG layer and a browser during the demo.

Author: Colile
"""
