"""
__init__.py - the knowledge-graph package.

`kg/model` holds the schema, `kg/loader` populates the graph from the three
required sources, and `kg/cypher` holds the demonstrated queries and the graph
service API. Imports no Phase 1 service; reads `soc/`'s stores and Phase 1's
configuration only.

Author: Colile
"""
